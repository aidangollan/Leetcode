class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        if len(nums) == 1:
            return nums[0]
        elif len(nums):
            return sorted(nums)[len(nums)//2]
        else:
            return 0
